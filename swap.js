const fs = require('fs'); let code = fs.readFileSync('c:/mern/black-panther/src/app/page.tsx', 'utf-8'); const contactRegex = /(\s*{\/\* Contact Us Section \*\/}.*?)(?=\s*{\/\* CTA Section \*\/})/s; const ctaRegex = /(\s*{\/\* CTA Section \*\/}.*?)(?=\s*<\/main>)/s; const contactMatch = code.match(contactRegex); const ctaMatch = code.match(ctaRegex); if(contactMatch && ctaMatch) { let newCode = code.replace(contactRegex, ''); newCode = newCode.replace(ctaMatch[0], ctaMatch[0] + contactMatch[0]); fs.writeFileSync('c:/mern/black-panther/src/app/page.tsx', newCode); console.log('Swapped successfully'); } else { console.log('Could not find matches'); }